CompileFlags:
  CompilationDatabase: build   # 原 --compile-commands-dir=${workspaceFolder}/build
Index:
  Background: Build            # 原 --background-index（默认已启用）
Completion:
  HeaderInsertion: Never       # 原 --header-insertion=never(需clangd 21)